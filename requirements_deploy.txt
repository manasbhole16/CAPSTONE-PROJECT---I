# requirements.txt — Capstone Project - I | Bluestock MF Analytics
# Core runtime dependencies (pinned for reproducibility)
# Install: pip install -r requirements.txt

pandas>=2.0,<4.0
numpy>=1.24,<3.0
matplotlib>=3.7,<4.0
seaborn>=0.12,<0.14
scipy>=1.10,<2.0
SQLAlchemy>=2.0,<3.0
requests>=2.28,<3.0
python-dotenv>=1.0,<2.0
reportlab>=4.0,<5.0
python-pptx>=0.6,<1.0

# Optional — notebook support
jupyter>=1.0
ipykernel>=6.0
ipywidgets>=8.0

# Optional — interactive dashboard (if converting to Streamlit)
# streamlit>=1.30
# plotly>=5.0
